# HQ FortiGate Configuration (Simplified)

config system interface
edit port1
set ip 192.168.100.1 255.255.255.0
next
end

config vpn ipsec phase1-interface
edit ADVPN-HUB
set interface port2
set auto-discovery-sender enable
set auto-discovery-forwarder enable
next
end
